spring:
  application:
    name: agenthub-backend

server:
  port: 8080

mybatis:
  mapper-locations: classpath*:mapper/**/*.xml

agenthub:
  adapters:
    default-type: MOCK
    codex:
      enabled: false
    claude-code:
      enabled: false
